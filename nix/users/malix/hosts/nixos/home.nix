# Host-specific Home Manager configuration for malix on nixos
# This file can be used to override or extend the base home.nix for this specific host
{ ... }:
{
  # Add host-specific home-manager configurations here
  # Currently empty as all settings are in the base home.nix
}
