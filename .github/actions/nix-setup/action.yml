name: "Nix Setup"
description: "Sets up Nix and Nix-taylored GitHub Caching"
runs:
  using: composite
  steps:
    - uses: DeterminateSystems/nix-installer-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main
      with:
        use-flakehub: false
